/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
        domains : ["github.com", "cloud.appwrite.io","lh3.googleusercontent.com"] //for image assets
    }
}

module.exports = nextConfig
